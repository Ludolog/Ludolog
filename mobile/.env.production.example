# Production Android build example.
# This value is public and is bundled into the mobile app.
# Replace with the real Vercel deployment URL before building release artifacts.
VITE_API_BASE_URL=https://gamevalue-radar.vercel.app
